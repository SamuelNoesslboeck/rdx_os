[package]
name = "rdx_os"
version = "0.1.0"
edition = "2024"

[dependencies]
embedded-hal = "1.0.0"
rppal = { version = "0.22.1", features = ["embedded-hal"] }

rdx_hal = { path = "./rdx_hal" }

[workspace]
members = [ "rdx_hal" ]
